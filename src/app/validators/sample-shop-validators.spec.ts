import { SampleShopValidators } from './sample-shop-validators';

describe('SampleShopValidators', () => {
  it('should create an instance', () => {
    expect(new SampleShopValidators()).toBeTruthy();
  });
});
